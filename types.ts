export type decodedType = {
  aud: string;
  auth_time: number;
  c_hash: string;
  email: string;
  email_verified: string;
  exp: number;
  iat: number;
  iss: string;
  nonce_supported: boolean;
  sub: string;
};
